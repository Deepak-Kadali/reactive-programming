export interface Car {
    name: string;
    model: string;
    yearOfRelease: number;
    brand: string;
    color: string;
}

export interface Scrap {
    brand: string,
    yearOfRelease: number,
}

